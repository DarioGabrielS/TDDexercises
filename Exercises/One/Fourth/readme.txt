Programa una función que dada una String te devuelva un Array de textos separados por cierto caracter.
    Ejemplo
         miFuncion('hola que tal', ' ') devolverá ['hola', 'que', 'tal'].


Program a function that separates a given String and returns an Array with the divided pieces of that String.